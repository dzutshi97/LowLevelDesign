package com.Uber;

public class MaxOfMinInEachWindow {

    //https://leetcode.com/problems/sliding-window-maximum/solutions/2059348/maximum-of-minimum-for-every-window-size/

    /**
     * int length = next[i] - prev[i] - 1;
     *
     *For each element arr[i],
     * the length of the window in which arr[i] is the minimum can be determined by the distance between its
     * next smaller element and previous smaller element.
     */
}
